{
  "private": true,
  "workspaces": ["talkietalker-stream-web", "packages/*"],
  "scripts": {
    "dev:web": "npm run dev --workspace=talkietalker-stream-web",
    "build:web": "npm run build --workspace=talkietalker-stream-web",
    "build:react": "npm run build --workspace=@talkietalker/stream-react"
  }
}
